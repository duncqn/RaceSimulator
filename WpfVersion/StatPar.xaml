<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfVersion"
        xmlns:WpfVersion="clr-namespace:WpfVersion" x:Class="WpfVersion.StatPar"
        mc:Ignorable="d"
        Title="StatPar" Height="450" Width="800">
    <Window.DataContext>
        <WpfVersion:DataContext/>
    </Window.DataContext>
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <DockPanel Grid.Column="0">
                <Label DockPanel.Dock="Top" Content="startime" />
                <ListView Margin="10" ItemsSource="{Binding TrackName}" />
            </DockPanel>
        </Grid>
    </Grid>
</Window>
